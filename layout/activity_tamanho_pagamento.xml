<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:orientation="vertical"
    android:padding="24dp"
    tools:context=".TamanhoPagamentoActivity">

    <!-- Titulo: Tamanho e valor da Pizza -->
    <TextView
        android:id="@+id/textTituloAviso"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Selecione o tamanho da pizza e o método de pagamento."
        android:textSize="20sp"
        android:textAlignment="center"
        android:textColor="#FF0000"
        android:textStyle="bold"
        android:layout_marginTop="16dp"/>

    <!-- Texto: Tamanho da Pizza -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Escolha o tamanho da pizza:"
        android:textSize="20sp"
        android:textStyle="bold"
        android:fontFamily="monospace"/>

    <!-- Grupo de Botões de Tamanho -->

    <RadioGroup
        android:id="@+id/rgTamanho"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RadioButton
            android:id="@+id/rbPequena"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="16sp"
            android:textColor="#000000"
            android:text="Pequena - R$20"/>

        <RadioButton
            android:id="@+id/rbMedia"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="16sp"
            android:textColor="#000000"
            android:text="Média - R$30"/>

        <RadioButton
            android:id="@+id/rbGrande"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="16sp"
            android:textColor="#000000"
            android:text="Grande - R$40"/>
    </RadioGroup>
    <!-- Texto: Pagamento -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Escolha o método de pagamento:"
        android:textSize="20sp"
        android:textStyle="bold"
        android:fontFamily="monospace"
        android:layout_marginTop="24dp"/>

    <!-- Grupo de Botões de Pagamento -->

    <RadioGroup
        android:id="@+id/rgPagamento"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RadioButton
            android:id="@+id/rbDinheiro"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="16sp"
            android:textColor="#000000"
            android:text="Dinheiro"/>

        <RadioButton
            android:id="@+id/rbCartao"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="16sp"
            android:textColor="#000000"
            android:text="Cartão"/>
    </RadioGroup>

    <!-- Botão - Confirmar Pedido -->

    <Button
        android:id="@+id/btnConfirmarPedido"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirmar Pedido"
        android:backgroundTint="#FF0000"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:onClick="confirmarPedido"/>
</LinearLayout>
